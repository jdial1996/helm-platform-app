apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-platform-app.fullname" . }}
  labels:
    {{- include "helm-platform-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-platform-app.selectorLabels" . | nindent 4 }}
